# 管理员功能需求文档与任务拆解

> 文档版本: v1.0
> 创建时间: 2025-11-24
> 状态: 📋 需求定义
> 依赖: 坐席认证系统 (v2.3.7+)

---

## 📋 文档概述

本文档定义了坐席管理系统的管理员功能需求，包括坐席账号 CRUD 操作、权限控制、审计日志等功能。此功能是坐席认证系统的扩展，旨在为管理员提供完整的坐席管理能力。

**关联文档**：
- `prd/01_全局指导/PRD_COMPLETE_v3.0.md` - 主 PRD 文档
- `prd/03_技术方案/api_contract.md` - API 接口契约
- `prd/02_约束与原则/CONSTRAINTS_AND_PRINCIPLES.md` - 约束17（坐席认证）

---

## 1. 需求背景

### 1.1 当前状态

坐席认证系统 v2.3.7 已实现基础功能：
- ✅ 坐席登录/登出
- ✅ JWT Token 认证
- ✅ 密码加密存储
- ✅ Token 刷新机制
- ✅ 3个默认账号

### 1.2 缺失功能

- ❌ 管理员创建新坐席账号
- ❌ 管理员修改坐席信息
- ❌ 管理员删除坐席账号
- ❌ 坐席自己修改密码
- ❌ 坐席列表查询
- ❌ JWT 权限中间件
- ❌ 角色权限控制
- ❌ 审计日志

### 1.3 业务价值

- 减少运维工作量：管理员可自助管理坐席
- 提高安全性：权限控制和审计日志
- 提升用户体验：坐席可自己修改密码

---

## 2. 功能需求

### 2.1 优先级定义

| 优先级 | 含义 | 交付时间 |
|-------|------|---------|
| P0 | 必须实现 | 本迭代完成 |
| P1 | 应该实现 | 下迭代完成 |
| P2 | 可以实现 | 待定 |

### 2.2 功能列表

#### P0 - 核心管理功能

| 功能ID | 功能名称 | 描述 | 角色 |
|--------|---------|------|------|
| ADMIN-01 | JWT 权限中间件 | 保护需要认证的 API | 系统 |
| ADMIN-02 | 坐席列表查询 | 管理员查看所有坐席 | admin |
| ADMIN-03 | 创建坐席账号 | 管理员创建新坐席 | admin |
| ADMIN-04 | 修改坐席信息 | 管理员修改坐席资料 | admin |
| ADMIN-05 | 删除坐席账号 | 管理员删除坐席 | admin |
| ADMIN-06 | 重置坐席密码 | 管理员重置密码 | admin |

#### P1 - 自助功能

| 功能ID | 功能名称 | 描述 | 角色 |
|--------|---------|------|------|
| ADMIN-07 | 修改自己密码 | 坐席修改自己的密码 | agent/admin |
| ADMIN-08 | 修改个人资料 | 坐席修改自己的信息 | agent/admin |

#### P2 - 高级功能

| 功能ID | 功能名称 | 描述 | 角色 |
|--------|---------|------|------|
| ADMIN-09 | 审计日志查询 | 查看操作历史 | admin |
| ADMIN-10 | 批量导入坐席 | CSV 批量创建 | admin |
| ADMIN-11 | 坐席状态监控 | 实时在线状态 | admin |

---

## 3. 详细需求

### 3.1 ADMIN-01: JWT 权限中间件

**优先级**: P0

**功能描述**：
创建 FastAPI 依赖项，验证请求中的 JWT Token，保护需要认证的 API。

**技术要求**：

```python
from fastapi import Depends, HTTPException
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

security = HTTPBearer()

async def verify_agent_token(
    credentials: HTTPAuthorizationCredentials = Depends(security)
) -> Dict[str, Any]:
    """验证 JWT Token 中间件"""
    token = credentials.credentials

    payload = agent_token_manager.verify_token(token)
    if not payload:
        raise HTTPException(401, detail="Token 无效或已过期")

    return payload

async def require_admin(
    agent = Depends(verify_agent_token)
) -> Dict[str, Any]:
    """要求管理员权限"""
    if agent.get("role") != "admin":
        raise HTTPException(403, detail="需要管理员权限")
    return agent
```

**需要保护的 API**：
- `GET /api/agents` - 坐席列表
- `POST /api/agents` - 创建坐席
- `PUT /api/agents/{username}` - 修改坐席
- `DELETE /api/agents/{username}` - 删除坐席
- `POST /api/agents/{username}/reset-password` - 重置密码
- `GET /api/sessions` - 会话列表
- `POST /api/sessions/{session_name}/takeover` - 接入会话
- `POST /api/manual/messages` - 发送消息

**验收标准**：
- [ ] 无 Token 请求返回 401
- [ ] 过期 Token 请求返回 401
- [ ] 无效 Token 请求返回 401
- [ ] 有效 Token 请求正常处理
- [ ] 普通坐席访问管理接口返回 403

---

### 3.2 ADMIN-02: 坐席列表查询

**优先级**: P0

**API 接口**：

```
GET /api/agents
Authorization: Bearer <admin_token>
```

**Query 参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| status | string | 否 | 过滤状态 (online/offline/busy) |
| role | string | 否 | 过滤角色 (admin/agent) |
| page | int | 否 | 页码，默认1 |
| page_size | int | 否 | 每页数量，默认20 |

**响应格式**：

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "agent_123",
        "username": "admin",
        "name": "系统管理员",
        "role": "admin",
        "status": "online",
        "max_sessions": 10,
        "created_at": 1763973603.8,
        "last_login": 1763974123.4
      }
    ],
    "total": 3,
    "page": 1,
    "page_size": 20
  }
}
```

**验收标准**：
- [ ] 返回所有坐席列表
- [ ] 不包含 password_hash
- [ ] 支持状态过滤
- [ ] 支持角色过滤
- [ ] 支持分页
- [ ] 仅管理员可访问

---

### 3.3 ADMIN-03: 创建坐席账号

**优先级**: P0

**API 接口**：

```
POST /api/agents
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**请求体**：

```json
{
  "username": "agent003",
  "password": "secure_password_123",
  "name": "客服小张",
  "role": "agent",
  "max_sessions": 5,
  "avatar_url": null
}
```

**响应格式**：

```json
{
  "success": true,
  "agent": {
    "id": "agent_1763980000000",
    "username": "agent003",
    "name": "客服小张",
    "role": "agent",
    "status": "offline",
    "max_sessions": 5,
    "created_at": 1763980000.0
  }
}
```

**错误响应**：

| 状态码 | 错误码 | 说明 |
|-------|-------|------|
| 400 | USERNAME_EXISTS | 用户名已存在 |
| 400 | INVALID_PASSWORD | 密码不符合要求 |
| 403 | FORBIDDEN | 非管理员 |

**密码要求**：
- 最少 8 个字符
- 包含字母和数字

**验收标准**：
- [ ] 成功创建坐席账号
- [ ] 密码正确加密存储
- [ ] 用户名唯一性检查
- [ ] 密码强度验证
- [ ] 仅管理员可创建

---

### 3.4 ADMIN-04: 修改坐席信息

**优先级**: P0

**API 接口**：

```
PUT /api/agents/{username}
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**请求体**：

```json
{
  "name": "客服张三",
  "role": "agent",
  "max_sessions": 8,
  "status": "online",
  "avatar_url": "/avatars/zhang.png"
}
```

**响应格式**：

```json
{
  "success": true,
  "agent": {
    "id": "agent_123",
    "username": "agent003",
    "name": "客服张三",
    "role": "agent",
    "status": "online",
    "max_sessions": 8
  }
}
```

**限制**：
- 不能通过此接口修改密码
- 不能修改 username
- 不能降级最后一个 admin

**验收标准**：
- [ ] 成功修改坐席信息
- [ ] 不能修改密码
- [ ] 不能修改用户名
- [ ] 保护最后一个管理员
- [ ] 仅管理员可修改

---

### 3.5 ADMIN-05: 删除坐席账号

**优先级**: P0

**API 接口**：

```
DELETE /api/agents/{username}
Authorization: Bearer <admin_token>
```

**响应格式**：

```json
{
  "success": true,
  "message": "坐席 agent003 已删除"
}
```

**限制**：
- 不能删除自己
- 不能删除最后一个 admin
- 不能删除有活跃会话的坐席

**验收标准**：
- [ ] 成功删除坐席
- [ ] 从 Redis 中删除数据
- [ ] 不能删除自己
- [ ] 保护最后一个管理员
- [ ] 检查活跃会话

---

### 3.6 ADMIN-06: 重置坐席密码

**优先级**: P0

**API 接口**：

```
POST /api/agents/{username}/reset-password
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**请求体**：

```json
{
  "new_password": "new_secure_password_456"
}
```

**响应格式**：

```json
{
  "success": true,
  "message": "密码已重置"
}
```

**验收标准**：
- [ ] 成功重置密码
- [ ] 新密码正确加密
- [ ] 密码强度验证
- [ ] 仅管理员可重置

---

### 3.7 ADMIN-07: 修改自己密码

**优先级**: P1

**API 接口**：

```
POST /api/agent/change-password
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**：

```json
{
  "old_password": "current_password",
  "new_password": "new_secure_password"
}
```

**响应格式**：

```json
{
  "success": true,
  "message": "密码修改成功"
}
```

**验收标准**：
- [ ] 验证旧密码正确
- [ ] 新密码强度验证
- [ ] 新密码不能与旧密码相同
- [ ] 任何登录用户可修改自己密码

---

### 3.8 ADMIN-08: 修改个人资料

**优先级**: P1

**API 接口**：

```
PUT /api/agent/profile
Authorization: Bearer <token>
Content-Type: application/json
```

**请求体**：

```json
{
  "name": "我的新名字",
  "avatar_url": "/avatars/my.png"
}
```

**限制**：
- 只能修改 name 和 avatar_url
- 不能修改 role、username、max_sessions

**验收标准**：
- [ ] 成功修改个人资料
- [ ] 只能修改允许的字段
- [ ] 任何登录用户可修改自己

---

### 3.9 ADMIN-09: 审计日志

**优先级**: P2

**功能描述**：
记录所有敏感操作的审计日志。

**记录内容**：
- 登录/登出
- 创建/删除坐席
- 修改密码
- 权限变更

**存储格式**：

```json
{
  "id": "log_123",
  "action": "agent_created",
  "operator": "admin",
  "target": "agent003",
  "details": {"role": "agent"},
  "timestamp": 1763980000.0,
  "ip_address": "192.168.1.100"
}
```

---

## 4. 任务拆解

### 4.1 P0 任务列表

| 任务ID | 任务名称 | 预估工时 | 依赖 |
|--------|---------|---------|------|
| ADMIN-T01 | 实现 JWT 权限中间件 | 2h | 无 |
| ADMIN-T02 | 实现坐席列表 API | 2h | T01 |
| ADMIN-T03 | 实现创建坐席 API | 2h | T01 |
| ADMIN-T04 | 实现修改坐席 API | 2h | T01 |
| ADMIN-T05 | 实现删除坐席 API | 2h | T01 |
| ADMIN-T06 | 实现重置密码 API | 1h | T01 |
| ADMIN-T07 | 编写单元测试 | 3h | T01-T06 |
| ADMIN-T08 | 更新 API 文档 | 1h | T01-T06 |
| ADMIN-T09 | 回归测试 | 1h | T07 |

**P0 总计**: 约 16 小时

### 4.2 P1 任务列表

| 任务ID | 任务名称 | 预估工时 | 依赖 |
|--------|---------|---------|------|
| ADMIN-T10 | 实现修改密码 API | 2h | P0 |
| ADMIN-T11 | 实现修改资料 API | 1h | P0 |
| ADMIN-T12 | 前端登录页面 | 4h | P0 |
| ADMIN-T13 | 前端坐席管理页面 | 6h | P0 |
| ADMIN-T14 | 前端权限控制 | 2h | T12 |

**P1 总计**: 约 15 小时

### 4.3 P2 任务列表

| 任务ID | 任务名称 | 预估工时 | 依赖 |
|--------|---------|---------|------|
| ADMIN-T15 | 审计日志模块 | 4h | P1 |
| ADMIN-T16 | 批量导入功能 | 3h | P1 |
| ADMIN-T17 | 状态监控面板 | 4h | P1 |

**P2 总计**: 约 11 小时

---

## 5. API 接口汇总

### 5.1 新增接口

| 方法 | 路径 | 功能 | 权限 |
|------|------|------|------|
| GET | /api/agents | 坐席列表 | admin |
| POST | /api/agents | 创建坐席 | admin |
| PUT | /api/agents/{username} | 修改坐席 | admin |
| DELETE | /api/agents/{username} | 删除坐席 | admin |
| POST | /api/agents/{username}/reset-password | 重置密码 | admin |
| POST | /api/agent/change-password | 修改自己密码 | any |
| PUT | /api/agent/profile | 修改个人资料 | any |

### 5.2 需要添加鉴权的现有接口

| 方法 | 路径 | 权限 |
|------|------|------|
| GET | /api/sessions | any |
| POST | /api/sessions/{session_name}/takeover | any |
| POST | /api/manual/messages | any |
| POST | /api/sessions/{session_name}/release | any |

---

## 6. 约束与限制

### 6.1 安全约束

- 所有管理接口必须验证 JWT Token
- 管理员操作必须验证 role=admin
- 密码必须使用 bcrypt 加密
- 敏感操作建议记录审计日志

### 6.2 业务约束

- 不能删除最后一个管理员
- 不能删除自己
- 不能删除有活跃会话的坐席
- 密码至少8字符，包含字母数字

### 6.3 性能约束

- 坐席列表查询必须支持分页
- 单页最多返回 100 条记录

---

## 7. 测试要求

### 7.1 单元测试

- JWT 中间件测试（有效/无效/过期 Token）
- 权限控制测试（admin/agent 角色）
- CRUD 操作测试
- 边界条件测试（最后一个管理员、活跃会话）

### 7.2 集成测试

- 完整的管理员操作流程
- 前后端集成测试
- 权限控制端到端测试

### 7.3 回归测试

- 确保不破坏现有功能
- 坐席认证基础功能正常
- 核心 AI 对话功能正常

---

## 8. 验收标准

### 8.1 功能验收

- [ ] JWT 权限中间件正常工作
- [ ] 管理员可以查看坐席列表
- [ ] 管理员可以创建新坐席
- [ ] 管理员可以修改坐席信息
- [ ] 管理员可以删除坐席
- [ ] 管理员可以重置密码
- [ ] 普通坐席无法访问管理接口

### 8.2 安全验收

- [ ] 无 Token 请求被拒绝
- [ ] 过期 Token 请求被拒绝
- [ ] 权限不足请求被拒绝
- [ ] 密码正确加密存储
- [ ] API 响应不包含密码

### 8.3 文档验收

- [ ] API 文档已更新
- [ ] 约束文档已更新
- [ ] 测试用例已编写
- [ ] 实施报告已创建

---

## 9. 风险评估

| 风险 | 影响 | 概率 | 缓解措施 |
|------|------|------|---------|
| 权限控制漏洞 | 高 | 中 | 完善测试用例 |
| 性能问题 | 中 | 低 | 分页和缓存 |
| 兼容性问题 | 中 | 低 | 回归测试 |

---

## 10. 附录

### 10.1 参考文档

- `prd/02_约束与原则/CONSTRAINTS_AND_PRINCIPLES.md` - 约束17
- `prd/03_技术方案/api_contract.md` - API 契约
- `docs/坐席认证系统实施报告.md` - 实施报告
- `docs/坐席认证系统答疑文档.md` - 答疑文档

### 10.2 更新历史

| 版本 | 日期 | 更新内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2025-11-24 | 初始版本 | Claude Code |

---

**文档版本**: v1.0
**最后更新**: 2025-11-24
**维护者**: Fiido AI 客服开发团队
